# AI 작업 규칙

이 저장소에서 작업하는 모든 AI 코딩 도구는 아래 규칙을 우선 적용한다. 목표는 빠른 코드 생성보다 **다른 사람과 AI의 변경을 보존하고 병합 충돌을 예방하는 것**이다.

## 작업 시작 전 필수 확인

1. `git status --short`와 현재 브랜치를 확인한다.
2. 사용자가 지정한 Issue, 목표, 허용 파일을 확인한다.
3. 최신 `develop`에서 해당 작업 전용 브랜치를 사용한다.
4. 이미 수정된 파일은 다른 작업자의 소유로 간주한다.
5. 수정 예정 파일이 다른 작업과 겹치거나 요구사항이 불명확하면 코드를 바꾸기 전에 사용자에게 알린다.

AI는 작업 시작 시 다음을 짧게 선언한다.

```text
목표:
수정 예정 파일:
수정하지 않을 파일:
DB/환경 변수/패키지 변경 여부:
검증 방법:
```

## 변경 범위

- 한 작업에서는 한 가지 기능 또는 한 가지 버그만 다룬다.
- 사용자가 허용한 파일과 목표 달성에 꼭 필요한 파일만 수정한다.
- 관련 없는 리팩터링, 파일명 변경, 폴더 이동, 전체 포맷팅을 하지 않는다.
- 기존 코드의 스타일을 따르고 주변 코드를 취향에 맞게 다시 작성하지 않는다.
- 기능 추가 시 가능하면 새 컴포넌트나 모듈에 코드를 격리한다.
- 공용 파일을 바꿔야 하면 최소한의 연결 코드만 추가한다.
- 사용자가 만든 미완성 변경, 주석, 임시 파일을 임의로 삭제하거나 되돌리지 않는다.
- `git reset --hard`, 광범위한 checkout/restore, 무단 stash를 사용하지 않는다.

## 동시 작업과 파일 소유권

다음 파일과 영역은 충돌 위험이 높으며 한 시점에 한 명 또는 한 AI만 담당한다.

- `app/MemoryTagApp.tsx`
- `app/globals.css`
- `package.json`, `package-lock.json`
- `db/schema.ts`
- `drizzle/`
- `.env.example`
- `AGENTS.md`, `CONTRIBUTING.md`

다른 브랜치나 작업자가 위 파일을 수정 중이면 AI는 동일 파일을 수정하지 않는다. 기능을 별도 파일로 구현한 뒤 공용 파일 연결은 지정된 통합 담당자가 수행한다.

여러 AI가 동시에 작업할 때는 AI마다 별도 브랜치와 별도 Git worktree를 사용한다. 같은 작업 폴더에서 두 AI를 동시에 실행하지 않는다.

```powershell
git fetch origin
git worktree add ..\McmMemorytag-issue-32 -b feature/32-login-validation origin/develop
```

worktree 생성과 삭제는 사람이 담당하며, AI는 사용자 승인 없이 다른 worktree를 삭제하지 않는다.

## 영역 분리

- UI 담당: `app/`의 지정 컴포넌트
- 인증 담당: `app/api/auth/`, `lib/auth.ts`
- DB 담당: `lib/db.ts`, `db/`, `drizzle/`
- 스타일 담당: 지정 CSS 범위
- 의존성 담당: `package.json`, `package-lock.json`

하나의 기능을 여러 AI가 나누면 DB → API → UI 순서의 작은 PR로 병합한다. 뒤 작업은 병합된 앞 작업을 기준으로 시작한다. 동시에 만들어야 한다면 인터페이스와 파일 경계를 Issue에 먼저 확정하고 서로의 파일을 수정하지 않는다.

## 데이터베이스

- DB 담당자로 지정된 AI만 스키마와 마이그레이션을 수정한다.
- 이미 병합된 마이그레이션을 수정하거나 삭제하지 않는다.
- 스키마 변경은 새 마이그레이션으로 추가한다.
- `db/schema.ts`, SQL 마이그레이션, Drizzle 메타데이터의 일관성을 확인한다.
- 파괴적인 변경에는 데이터 보존 및 복구 방법을 먼저 제시한다.
- 실제 공용 DB에 마이그레이션을 실행하지 않는다. 실행은 지정된 사람이 담당한다.
- 테스트를 위해 공용 데이터를 삭제하거나 초기화하지 않는다.

## 패키지와 생성 파일

- 사용자의 명시적 허용 없이 패키지를 추가, 삭제, 업그레이드하지 않는다.
- 패키지 변경 담당 AI만 `package-lock.json`을 수정한다.
- lock 파일 충돌을 손으로 부분 병합하지 않는다. 최종 `package.json`을 기준으로 다시 생성한다.
- 빌드 결과물과 자동 생성 파일을 불필요하게 커밋하지 않는다.
- 코드 생성 명령은 해당 생성 파일의 단독 담당일 때만 실행한다.

## 환경 변수와 보안

- `.env`와 실제 비밀값을 읽어 응답에 출력하거나 커밋하지 않는다.
- 새 설정은 `.env.example`에 가짜 예시 또는 설명만 추가한다.
- DB 비밀번호, 쿠키, 토큰, API 키를 로그, Issue, PR에 남기지 않는다.
- 인증 코드는 비밀번호 해시, 파라미터 SQL, HttpOnly 쿠키 원칙을 유지한다.

## Git 사용

- `main`과 `develop`에 직접 커밋하거나 push하지 않는다.
- 다른 사람의 커밋을 수정하거나 히스토리를 재작성하지 않는다.
- rebase는 본인만 사용하는 작업 브랜치에서만 한다.
- push, PR 생성, 병합, 브랜치 삭제는 사용자가 요청한 경우에만 수행한다.
- 커밋을 요청받으면 현재 작업과 관계된 파일만 stage한다.

## 충돌 발견 시

AI는 충돌을 자동으로 밀어붙이지 않고 다음 정보를 사용자에게 보고한다.

```text
충돌 파일:
내 작업의 의도:
상대 변경에서 보존해야 할 내용:
안전한 해결 제안:
추가 확인이 필요한 결정:
```

`ours` 또는 `theirs`로 파일 전체를 선택하지 않는다. 양쪽 변경의 의도를 확인할 수 없으면 작업을 멈춘다.

## 검증과 완료 보고

변경 후 최소한 다음을 실행한다.

```powershell
npm run lint
npm run build
git diff --check
git status --short
```

실제 DB 정보가 없어 실행하지 못한 통합 테스트는 성공했다고 표현하지 않는다.

완료 보고에는 다음을 포함한다.

- 구현한 내용
- 실제 수정한 파일
- 실행한 검증과 결과
- 실행하지 못한 검증
- DB, 환경 변수, 패키지 변경
- 다음 작업자가 알아야 할 사항

## AI에게 작업을 요청하는 권장 형식

```text
Issue: #32
브랜치: feature/32-login-validation
목표: 로그인 입력값 검증 추가
허용 파일:
- app/components/LoginForm.tsx
- lib/auth-validation.ts
금지 파일:
- app/MemoryTagApp.tsx
- app/globals.css
- package.json
- package-lock.json
DB 변경: 없음
패키지 변경: 금지
완료 조건:
- 잘못된 입력에 오류 표시
- 기존 로그인 유지
- lint/build 통과
주의:
- 기존 미커밋 변경을 수정하거나 삭제하지 말 것
- 범위 밖 변경이 필요하면 먼저 보고할 것
```
